import styled from 'styled-components';

const Form = styled.form`
  display: flex;
  flex-direction: column;
  gap: 0.8rem;

  align-items: center;

  button {
    margin-top: 1.6rem ;
  }
`;

export default Form;
